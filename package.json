{
  "name": "faceguard-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:release-site": "npm run build && node scripts/prepare-release.mjs",
    "android:sync": "npm run build && cap sync android",
    "android:open": "cap open android",
    "start": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@supabase/supabase-js": "2.110.5"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2",
    "vite": "8.1.4"
  }
}
