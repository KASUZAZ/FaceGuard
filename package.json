{
  "name": "faceguard-app",
  "version": "2.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:release-site": "vite build && node scripts/prepare-release.mjs",
    "android:sync": "vite build && cap sync android",
    "android:open": "cap open android",
    "start": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@supabase/supabase-js": "2.110.5"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2",
    "vite": "8.1.4"
  }
}
